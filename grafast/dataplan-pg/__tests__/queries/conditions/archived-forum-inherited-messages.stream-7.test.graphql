## expect(errors).toBeFalsy()
## expect(queries).toHaveLength(3);
{
  forums(includeArchived: EXCLUSIVELY) {
    name
    messagesConnection(includeArchived: INHERIT) {
      # Non-streamed edges need the shared rows up front; deferred pageInfo also
      # requires materialization. Only GraphQL delivery can still stream.
      nodes @stream {
        body
        author {
          username
          gravatarUrl
        }
      }
      # No @stream here. `edges` and `nodes` should still deduplicate and the
      # list should be populated from this without streaming even though the
      # results may still stream
      edges {
        cursor
        node {
          author {
            username
          }
        }
      }
      ... @defer {
        pageInfo {
          hasNextPage
          hasPreviousPage
        }
        totalCount
      }
    }
  }
}
