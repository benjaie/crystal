## expect(errors).toBeFalsy()
## expect(queries).toHaveLength(4);
# PERF: should be able to change queries down to 7 by inlining author, but only
# because `pageInfo` was not requested.
{
  forums(includeArchived: EXCLUSIVELY) {
    name
    messagesConnection(includeArchived: INHERIT, last: 2) {
      # Backward pagination currently fetches the full requested page and reverses
      # it before delivery, so database streaming is disabled.
      nodes @stream(initialCount: 1) {
        body
        author {
          username
          gravatarUrl
        }
      }
    }
  }
}
